import { CustomSetterFormItemProps } from 'src/types/setter'
import React from 'react'

const RadiusSetter: React.FC<CustomSetterFormItemProps> = (
) => {
  return (
    <div>1</div>
  )
}

export default React.memo(RadiusSetter)
