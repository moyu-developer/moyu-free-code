import type { NextPage } from "next";
import Head from "next/head";
import { Layout } from "@douyinfe/semi-ui";
import OperationBar from "./components/OperationBar";
import ProductSider from './components/ProductSider';
import Canvas from './components/Canvas'
import Configuration from './components/Configuration'
import styles from "@/styles/layout.module.css";
import { DndProvider } from 'react-dnd'
import { HTML5Backend } from 'react-dnd-html5-backend'
import './model'
import { useSelector } from "react-redux";
import type { RootState } from "@/common/model";

const { Content } = Layout;

const Desktop: NextPage = () => {

  /** @name dslView 页面结构 */
  const dslView = useSelector((state: RootState) => state.schema.dslView)

  return (
    <Layout className={styles["app"]}>
      <Head>
        <title>{ dslView.viewName }</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <OperationBar />
      <Layout className="render_container">
				<DndProvider backend={HTML5Backend}>
        <ProductSider/>
        <Content>
          <Canvas/>
        </Content>
        <Configuration/>
        </DndProvider>
      </Layout>
    </Layout>
  );
};

export default Desktop;
